# Copy to .env and fill in values
PORT=3000
JWT_SECRET=change-this-to-a-long-random-secret
DB_PATH=./data/factory.db

# Anthropic API key for AI features
ANTHROPIC_API_KEY=sk-ant-...

# Store URL suffix (stores get URL: store-<id>.<suffix>)
STORE_URL_SUFFIX=localhost

# Optional: force in-cluster K8s config (set to 'true' when deployed to K8s)
# K8S_IN_CLUSTER=true
